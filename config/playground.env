# Default workflow image used by the Kaizen Playground dashboard paths.
#
# Keep this tag stable for ordinary dashboard users. The Publish to Playground
# path updates this image, and remote workflow manifests use imagePullPolicy:
# Always so new launches pick up the latest pushed image for this tag.
CADS_WORKFLOW_IMAGE=ghcr.io/janlv/cads-fmi-demo:playground
