services:
  orchestrator:
    build: .
    image: cads-fmi-demo:latest
    container_name: cads-fmi-orchestrator
    volumes:
      - ./data:/app/data
      - ./config:/app/config:ro
    command: ["python", "orchestrator/run.py"]
